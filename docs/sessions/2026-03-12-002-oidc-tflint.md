# Session 2026-03-12-002 — oidc-tflint

**Branch:** fix/2026-03-12-002-oidc-tflint
**Issue:** #40, #11
**PR:** (fill in when created)
**Outcome:** completed

## Objective

- #40: Document the OIDC `pull_request` federated credential fix as a runbook (no code change needed — Azure Portal action)
- #11: Add `tflint` step to `workload-azure.yaml` and `workload-dbx.yaml` CI workflows

## What was done

- Created `docs/runbooks/oidc-pull-request-credential.md` with step-by-step Azure Portal instructions for adding the `pull_request` federated credential
- Added `tflint` steps to both `workload-azure.yaml` and `workload-dbx.yaml` using the `terraform-linters/setup-tflint@v4` action
- tflint runs after `Terraform Init` on all events (PR, push to main, workflow_dispatch); no .tflint.hcl required for basic checks

## Decisions

- tflint added without a .tflint.hcl plugin config — basic checks are sufficient for now; provider-specific plugins can be added in a follow-on
- tflint step runs unconditionally (not gated on `pull_request` only) so it catches issues on main pushes too

## Artifacts

- `docs/runbooks/oidc-pull-request-credential.md` (new)
- `.github/workflows/workload-azure.yaml` (tflint step added)
- `.github/workflows/workload-dbx.yaml` (tflint step added)
- `docs/sessions/2026-03-12-002-oidc-tflint.md` (this file)
